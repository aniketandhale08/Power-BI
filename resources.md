### Steps to follow in data transformation
1. Use the first row as a header
2. Date transformation (Year, months, weeks, days)
3. Data type
4. Name the tables after importing
5. Merge columns/ Split columns/ Trim
6. Remove unwanted columns
7. Remove duplicate
8. Deal with Null values/ make actuals in the column if they are not (format: null), use the replace function.
9. Change the order/ sequence of columns


#### Date & Time

